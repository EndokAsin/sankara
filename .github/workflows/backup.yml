name: Supabase Backup

on:
  schedule:
    - cron: "0 0 * * *"   # otomatis jalan tiap hari jam 00:00 UTC
  workflow_dispatch:      # bisa dijalankan manual dari tab Actions

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Jalankan backup lewat Edge Function
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            https://<vfdxtujestpslpsvdkwh>.functions.supabase.co/backup
